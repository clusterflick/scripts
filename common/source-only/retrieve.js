async function retrieve() {
  // This venue does not have a site to retrieve events from - instead these
  // events from from external soruces which will be handled at transform.
  return {};
}

module.exports = retrieve;
