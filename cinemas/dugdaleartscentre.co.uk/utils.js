// The venue's what's-on, filtered to film. Shared with the health probe, which
// reads the same listing the retrieve starts from.
const LISTING_LINK = ".whats-on-grid .event .event-name a";

module.exports = { LISTING_LINK };
