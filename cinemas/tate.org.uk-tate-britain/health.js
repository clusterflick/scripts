// Both Tate galleries are probed the same way on their own what's-on - see
// common/tate.org.uk/health.js. Attributes arrive from the caller, so unlike
// `retrieve` there is nothing to bind here.
module.exports = require("../../common/tate.org.uk/health");
